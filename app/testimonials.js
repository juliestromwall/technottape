/**
 * Real client testimonials only.
 *
 * The site is live, so anything in here is read by actual prospects as a
 * genuine endorsement. Invented quotes attributed to clients are false
 * advertising — in the US the FTC treats fabricated endorsements as
 * deceptive, and a prospect who discovers one stops believing everything
 * else on the page.
 *
 * The section below renders nothing while this array is empty, so the page
 * is complete and simply has no testimonials block yet. Paste real ones in
 * and it appears. See docs/TESTIMONIALS.md for a request template.
 *
 *   { quote: 'What they actually said.',
 *     name: 'First name + initial, or full name with permission',
 *     role: 'Owner, Business Name',
 *     accent: 'var(--sage)' | 'var(--ochre)' | 'var(--terracotta)' }
 */
export const testimonials = [];
